#include "holberton.h"
/**
 * _isupper - checks for upper letters
 * Return: the absolute value of an integer
 * @c: number to be proccesed
 */
int _isdigit(int c)
{
	if (c >= 48 && c <= 60)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
